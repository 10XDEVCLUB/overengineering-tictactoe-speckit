# overengineering-tictactoe-speckit Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-15

## Active Technologies
- Java (JDK 26, Azul Zulu — pre-GA; merges into `jdk26` branch) + JDK built-in only — `com.sun.net.httpserver`, (014-http3-game-server)
- In-memory (ConcurrentHashMap for game sessions) (014-http3-game-server)

- Java (JDK 26, Azul Zulu — pre-GA; `jdk26` branch) + JDK built-in only — `com.sun.net.httpserver`, (014-http3-game-server)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Java (JDK 26, Azul Zulu — pre-GA; `jdk26` branch)

## Code Style

Java (JDK 26, Azul Zulu — pre-GA; `jdk26` branch): Follow standard conventions

## Recent Changes
- 014-http3-game-server: Added Java (JDK 26, Azul Zulu — pre-GA; merges into `jdk26` branch) + JDK built-in only — `com.sun.net.httpserver`,

- 014-http3-game-server: Added Java (JDK 26, Azul Zulu — pre-GA; `jdk26` branch) + JDK built-in only — `com.sun.net.httpserver`,

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
